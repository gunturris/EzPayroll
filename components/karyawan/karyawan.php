<?php
/*
	Files generated by Kornea auto 
	On : 2015-03-14 10:03:33
*/	
my_component_load('__jsload' , false);
my_component_load('__paging' , false);  
my_component_load('karyawan' );
$task = isset($_GET['task']) ? $_GET['task'] : ''; 
$id = isset( $_GET['id'] ) ? $_GET['id']:  0;
$modulname = 'Konfigurasi <i class="fa fa-caret-right fa-fw"></i> Data Referensi <i class="fa fa-caret-right fa-fw"></i> Karyawan  ';

if($_SERVER['REQUEST_METHOD'] == "POST" ):
 	switch($task){
		case   "edit" :
			$validatepost = form_karyawan_validate($id);
			if($validatepost){
				$errors = message_multi_error($validatepost);
				$content = $errors;
				$content .= edit_karyawan($id); 
			}else{
				submit_karyawan($id);
				$content =  "Updated";
				my_direct('index.php?com='.$_GET['com']);
			 }
			break; 
	}
else: 	
	if($task == "edit"){ 
		$content =  edit_karyawan($id) ;
	}elseif($task == "delete"){ 
		my_delete_record('karyawan','karyawan_id', $id);
		my_direct($_SERVER['HTTP_REFERER']);	
	}else{ 
		load_facebox_script();
		$content =  list_karyawan() ; 
	}
endif; 
generate_my_web($content, $modulname );
?>
